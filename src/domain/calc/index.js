import { allocateItemsToPeople } from './allocations'
import {roundAndDistribute} from './rounding'

/**
 * Main entry later:
 * computeBill(billSettings, items, people) -> per-person breakdown
 */
// export function computeBill(...) { ... }